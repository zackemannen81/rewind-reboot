; MCP server for editor tooling. Autostart so a clone that enables the
; ModelContextProtocol plugin listens on the same local port as the
; committed client configs under .mcp.json and editor folders.
[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
ServerUrlPath=/mcp
ServerPortNumber=8000
bAutoStartServer=True
bEnableToolSearch=True
